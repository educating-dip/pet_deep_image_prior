hydra:
  run:
    dir: ./${dataset.name}/${prior.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./${dataset.name}/${prior.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.override_dirname}

defaults:
  - dataset: 2D_low
  - prior: OSEM
